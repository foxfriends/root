export default class Board {
  constructor(
    name,
    clearings,
    paths,
    rivers,
  ) {
    this.name = name;
    this.clearings = clearings;
    this.paths = paths;
    this.rivers = rivers;
  }
}
